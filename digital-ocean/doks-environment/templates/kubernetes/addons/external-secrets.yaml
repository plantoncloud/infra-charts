{% if values.externalSecretsEnabled | bool %}
---
apiVersion: kubernetes.project-planton.org/v1
kind: KubernetesExternalSecrets
metadata:
  name: {{ values.cluster_name }}-external-secrets
  group: security-addons
  relationships:
    - kind: DigitalOceanKubernetesCluster
      name: "{{ values.env }}-{{ values.cluster_name }}"
      type: runs_on
spec:
  targetCluster:
    kubernetesClusterSelector:
      clusterKind: DigitalOceanKubernetesCluster
      clusterName: "{{ values.env }}-{{ values.cluster_name }}"
  container:
    resources:
      limits:
        cpu: 1000m
        memory: 1Gi
      requests:
        cpu: 50m
        memory: 100Mi
{% endif %}

